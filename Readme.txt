📝 Handwritten Character Recognition

A deep learning project that recognizes handwritten letters and digits using a CNN model trained on the EMNIST dataset.

🚀 Features

Predicts handwritten A–Z characters and 0–9 digits

Built using Convolutional Neural Network (CNN)

Simple and easy to extend

📂 Files

app.py – main script

model.h5 – trained model

requirements.txt – dependencies


📌 Dataset

Uses the EMNIST Letters dataset.
Add dataset inside the dataset/ folder.

📈 Model

CNN model with convolution, pooling, dense layers, and softmax output.

🔮 Future Scope

Word and sentence recognition

GUI or real-time prediction


To run app.py from VS code run this command 
 streamlit run your_path/app.py


